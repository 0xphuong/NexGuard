#!/bin/sh
set -e
source "$(dirname -- "$0")/bootstrap"

./nexguard eval FzHttp.Release.migrate

if [ "$RESET_ADMIN_ON_BOOT" = "true" ]; then
  ./nexguard eval FzHttp.Release.create_admin_user
fi

exec ./nexguard start
