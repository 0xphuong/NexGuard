#!/bin/sh
<% if node['nexguard']['logging']['enabled'] -%>
  echo "received TERM from runit, sending INT instead to force quit connections"
<% end -%>
<%= node['runit']['sv_bin'] %> interrupt postgresql
